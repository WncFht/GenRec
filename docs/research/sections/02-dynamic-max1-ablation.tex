\section{Hint 家族内部对照}

\subsection{dynamic sid-only vs canonical dynamic gather-fix}

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{4.0cm} p{2.5cm} c c c c c}
\toprule
Variant & Best ckpt & Epoch & NDCG@10 & HR@10 & NDCG@50 & HR@50 \\
\midrule
dynamic sid-only & checkpoint-2394 & 1.805 & 0.0921 & 0.1155 & 0.1065 & 0.1830 \\
dynamic gather-fix & checkpoint-2997 & 1.802 & 0.0936 & 0.1169 & 0.1083 & 0.1855 \\
\bottomrule
\end{tabular}
\caption{dynamic sid-only 与 canonical dynamic gather-fix。}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth]{rl-dynamic-family-curves.png}
\caption{dynamic family 曲线，后文也用这张图讨论 ranking dynamic。}
\end{figure}

这里最重要的不是 “sid-only 有没有收益”，而是：

\begin{itemize}
\item \texttt{dynamic sid-only} 并不是完全失败，但它的 peak 来得太早，后面没有继续抬高。
\item \texttt{dynamic gather-fix} 四个主指标同时更强，因此应该被视为当前默认的 dynamic 实现。
\end{itemize}

\subsection{fixed sid-only vs fixed taskfix}

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{4.0cm} p{2.5cm} c c c c c}
\toprule
Variant & Best ckpt & Epoch & NDCG@10 & HR@10 & NDCG@50 & HR@50 \\
\midrule
fixed taskfix & checkpoint-2997 & 1.802 & 0.0931 & 0.1189 & 0.1094 & 0.1941 \\
fixed taskfix sid-only & checkpoint-2652 & 2.000 & 0.0945 & 0.1205 & 0.1103 & 0.1935 \\
old fixed mixed-single & checkpoint-3326 & 2.000 & 0.0953 & 0.1193 & 0.1114 & 0.1938 \\
\bottomrule
\end{tabular}
\caption{fixed taskfix、fixed taskfix sid-only 与 old fixed 的位置。}
\end{table}

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth]{fixed-hint-taskfix-sid-only-epoch-curves.png}
\caption{fixed sid-only 与 fixed taskfix 的对照图。}
\end{figure}

这组图和表对应的主结论是：

\begin{itemize}
\item \texttt{fixed taskfix sid-only} 主要把 top-10 hit 又往上推了一步，因此它是更合适的 clean fixed reference。
\item \texttt{fixed taskfix} 本身已经明显强于 plain \texttt{rule-only}，说明 task-aware 修复本身就在生效。
\item old fixed 仍然可以当历史上界参考，但不应该继续当方法定义。
\end{itemize}

\subsection{ranking dynamic vs canonical dynamic}

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{4.0cm} p{2.5cm} c c c c c}
\toprule
Variant & Best ckpt & Epoch & NDCG@10 & HR@10 & NDCG@50 & HR@50 \\
\midrule
ranking dynamic & checkpoint-1665 & 1.001 & 0.0911 & 0.1138 & 0.1058 & 0.1822 \\
dynamic gather-fix & checkpoint-2997 & 1.802 & 0.0936 & 0.1169 & 0.1083 & 0.1855 \\
\bottomrule
\end{tabular}
\caption{ranking dynamic 与 canonical dynamic gather-fix。}
\end{table}

在动态家族里，\texttt{ranking dynamic} 当前没有显示出独立价值：

\begin{itemize}
\item 按 best \texttt{NDCG@10} 点比较，它四指标同时落后于 \texttt{dynamic gather-fix}。
\item 图上也能直接看到它更像 “中期见顶，后期失速” 的 run，而不是新的主线实现。
\end{itemize}

\subsection{错版 fixed vs corrected fixed：old fixed 为什么不能直接拿来当方法}

\begin{figure}[H]
\centering
\includegraphics[width=0.92\textwidth]{fixed-hint-bug-task-depth-distribution.png}
\caption{old fixed 的 bug 会把不同任务的 hint depth 分布系统性压歪。}
\end{figure}

\begin{table}[H]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{p{3.2cm} c c c p{4.1cm}}
\toprule
Task & Changed rate & Avg depth correct & Avg depth buggy & 主效应 \\
\midrule
\texttt{task1\_sid\_sft} & 7.64\% & 1.048 & 0.938 & 只被轻微压浅 \\
\texttt{task4\_hisTitle2sid} & 82.35\% & 1.533 & 0.188 & hardest task 几乎被压成 depth-0 \\
\texttt{task5\_title\_desc2sid} & 19.41\% & 0.519 & 0.245 & 一部分 depth-2 被压回 0/1 \\
\bottomrule
\end{tabular}
\caption{old fixed 的 bug 不是“hint 更准”，而是把任务分布系统性压浅。}
\end{table}

这部分的重点不是再证明 old fixed 数值不错，而是解释为什么它不能直接拿来当方法结论：

\begin{itemize}
\item old fixed 的表面优势，不是来自更正确的 hint map，而是因为 hardest task 被系统性少给 hint。
\item 因此它更像一个带偏置的历史现象学上界，而不是应该继续复用的方法定义。
\item 这也是为什么本文后文在需要 fixed reference 时，默认优先引用 corrected \texttt{fixed taskfix sid-only}。
\end{itemize}
